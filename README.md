# Supabase Upload Template

Cara pakai:
1. Clone repo
2. Copy `.env.local.example` → `.env.local` dan isi credential Supabase
3. Jalankan `npm install`
4. Jalankan `npm run dev` untuk mode development
5. Deploy ke Vercel (pastikan environment variables di Vercel sudah diisi)

Pastikan kamu sudah membuat Bucket di Supabase Storage (contoh bucket name: `uploads`).
